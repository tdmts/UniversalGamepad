<Page
    x:Class="UniversalGamepad.GUI.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UniversalGamepad.GUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Vertical">
                <Button x:Name="btnStart" Click="btnStart_Click" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">Start</Button>
                <ScrollViewer x:Name="scvOutput" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Auto" Height="300">
                    <TextBox x:Name="lblOutput" AcceptsReturn="True"></TextBox>    
                </ScrollViewer>
            </StackPanel>
            
        </StackPanel>
        
        
    </Grid>
</Page>
